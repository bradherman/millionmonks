<% 
#	unless wisdom.user == current_user ||
#	!current_user ||
#	current_user.voted_on?(wisdom)
  if !current_user || (!(wisdom.user == current_user) && !current_user.voted_on?(wisdom))
%>
	<li><%= link_to "&#8226; Upvote".html_safe, wisdom_vote_path(:id => wisdom, :vote => :true),:remote => true
	%></li> 
	
	<li><%= link_to "&#8226; Flag".html_safe, wisdom_vote_path(:id => wisdom, :vote => :false),:remote => true
 	%> </li>

<% end %>
<li>&#8226; Karma: <%= wisdom.karma %></li>